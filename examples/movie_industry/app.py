"""Movie Industry app using recon-graphrag SDK.

Two entry points:
  - build_graph.py  — ingest data and build communities
  - query.py        — search the graph with domain-specific prompts

Shared config lives in config.py.
"""
